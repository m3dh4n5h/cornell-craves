{
  "//": "Declaration-only emit for design-sync. The app itself is noEmit, so without this the converter finds no .d.ts and every component's props interface comes out empty — which is what the design agent codes against. Regenerated by buildCmd; output is gitignored.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "emitDeclarationOnly": true,
    "outDir": "types",
    "rootDir": "../src",
    "allowImportingTsExtensions": false,
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "include": ["../src/**/*.ts", "../src/**/*.tsx"],
  "exclude": ["../src/mock/**"]
}
